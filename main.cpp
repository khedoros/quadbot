#include<iostream>
#include "input/ControlInput.h"
#include "robot/ServoController.h"

int main() {
    //Determine control method (USB control? Text input? Web page? AI?)
    //Instantiate control method (Feeds control data to the robot)
    //Instantiate robot controller (takes input of high-level control commands, outputs servo position commands
    //Surrender control of program to main control loop
}
